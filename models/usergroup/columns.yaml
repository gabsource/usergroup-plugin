# ===================================
#  Column Definitions
# ===================================

columns:

    id:
        label: bnb.usergroup::lang.group.id
        invisible: true

    name:
        label: bnb.usergroup::lang.group.name
        searchable: true

    code:
        label: bnb.usergroup::lang.group.code

    created_at:
        label: bnb.usergroup::lang.group.created_at

    users_count:
        label: backend::lang.user.group.users_count
        relation: users_count
        valueFrom: count
        default: 0
